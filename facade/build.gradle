apply plugin: 'com.android.library'

android {
    compileSdkVersion 28
    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 28
    }
}

dependencies {
    // manifest is merged in app (works with api dep as well)
    implementation project(':internal')

    // not merged!
    implementation 'com.facebook.android:facebook-core:4.34.0'

    // manifest is merged in app this way only
    // api 'com.facebook.android:facebook-core:4.34.0'
}
